export enum Time {
  Month = "month",
  Year = "year",
}

export enum MonthOrYear {
  Month = "month",
  Year = "year",
}

export enum ContractType {
  Profile = "profit",
  Revenue = "revenue",
}

export enum ContractStatus {
  Pending = "pending",
  Active = "active",
}
